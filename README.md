# Lists (`list-array`, formerly `list`s)

## Usage

```js
const List = require('list-array')
var list = new List()

list.push('this')
list.push('works')

console.log(list.random()) // => 'this' or 'works'
```
